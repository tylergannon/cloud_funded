require 'spec_helper'

describe Admin::MembersController do
  before :each do
    sign_in_as_admin
  end
  describe "put #update" do
    describe "when setting the administrator bit" do
      before :each do
        
      end
    end
  end
end