%h1 Project Administration
%table
  - @projects.each do |project|
    %tr
      %td= link_to project.name, project
      %td= image_tag project.image.url(:medium), width: '70px'
      %td= link_to 'Delete', admin_project_path(project), method: :delete, data: {confirm: 'Really?  It will be gone gone.'}
      %td= link_to 'Edit', '/editor' + project_path(project)
      %td= link_to 'Send Mail', "mailto:#{project.owner.email}"
