%h1 Update your profile

= form_for @member, as: :profile, url: profile_path(@member) do |f|
  .field
    = f.label :first_name
    = f.text_field :first_name
  .field
    = f.label :last_name
    = f.text_field :last_name
  = f.submit