%h1 Update your profile

= form_for @member, as: :profile, builder: MoneyFormBuilder, url: profile_path(@member) do |f|
  = f.control_group_email_field :email
  = f.control_group_text_field :first_name
  = f.control_group_text_field :last_name
  = f.control_group_text_field :google_plus
  = f.submit
