
<%= link_to '#', class: "share_pledge btn btn-large btn-primary #{'disabled' if shared_pledge?}", data: {object_url: project_url(project), object_type: project.class.name, object_id: project.id} do %>
  
  <h3><%= shared_pledge? ? "Shared!" : "Share on FB!"%></h3>
<% end %>

