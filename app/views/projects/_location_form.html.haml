= f.control_group_text_field :address, label: 'Address, City or Zip', popover: { title: 'Address', content: 'Where are you?  Exact address, or city and state, even zip will do.', trigger: 'focus' }
= button_tag 'Check Address', id: 'check_address'
= f.hidden_field :lat
= f.hidden_field :long
= f.hidden_field :street_number
= f.hidden_field :route
= f.hidden_field :city
= f.hidden_field :county
= f.hidden_field :state
= f.hidden_field :postal_code
#answer_wrapper
#map_canvas.span8

= content_for :head do
  = javascript_include_tag 'https://maps.googleapis.com/maps/api/js?sensor=false'
